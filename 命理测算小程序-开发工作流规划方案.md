# 命理测算小程序 · 整体开发工作流规划方案

> 基于已安装的技能 `fortune-master-pro-dao-v2`（v2.0.0，全体系增强版智能命理师）所做的架构分析 + 落地开发规划。
> 适用读者：产品/技术负责人、接手开发的工程师。本文档可直接作为开发立项与排期基线。

---

## 0. 最关键的前置澄清：这个技能没有"代码 DAO 层"

在规划"对接现有 dao 层"之前，必须先纠正一个命名误解，否则整个架构会建在错误地基上：

- **`fortune-master-pro-dao-v2` 不是代码工程**，而是一个 **领域知识 + 提示词框架型技能（prompt-template / domain-specific skill）**。
- 它**全部由 Markdown 文件构成**（1 个 `SKILL.md` + 13 个 `references/*.md` + `README`/`CHANGELOG`/`_meta.json`），**不含任何可执行代码、函数、Class、API 或数据访问对象**。
- 名字里的 **`-dao-` 是"道家"玄学分流（道家 = Daoist），不是软件里的 Data Access Object**；`-v2` 是版本号。
- 因此**不存在可 import 的"dao 层"**。它真正的资产是一整套**结构化的命理领域知识（规则、框架、分级、模板、护栏）**。

**结论性重述**：所谓"核心测算逻辑与现有 dao 层的对接方式"，正确的理解应是——

> **把该技能的文本知识层，提升为小程序里的【知识/规则引擎】+【AI 解读 Prompt 层】，而不是去对接一个并不存在的数据访问层。**

本方案第 5 节专门给出"如何把 Markdown 知识层翻译成可消费代码资产"的对接方法。

---

## 1. fortune-master-pro-dao-v2 架构与能力盘点（分析结论）

### 1.1 已有的命理测算模块（10 大体系 + 关系专题）

| # | 体系 | 输入要素（最少→完整） | 核心分析维度（来自 framework） | 对应文件 |
|---|------|----------------------|-------------------------------|----------|
| 1 | 八字 / 四柱 | 公历生日+时辰+出生地 | 日主、五行、十神、格局、用神、阶段主题 | `bazi-framework.md` |
| 2 | 紫微斗数 | 生日+时辰+出生地+性别（或命盘截图） | 命宫/身宫、十二宫、主星、四化 | `ziwei-framework.md` |
| 3 | 塔罗 | 牌阵+每张牌+正逆位+问题（或象征式抽牌） | 牌义、位置、正逆位、牌间关系 | `tarot-framework.md` |
| 4 | 西方星盘 | 生日+时间+出生地 | 日月升、行星落宫、相位、合盘 | `astrology-framework.md` |
| 5 | 数字命理 | 出生年月日（姓名可选） | 生命灵数、生日数、阶段循环 | `numerology-framework.md` |
| 6 | 奇门遁甲 | 问题+起局时间（或盘面截图） | 值符/值使/门星神、局势、动法、窗口 | `qimen-framework.md` |
| 7 | 六爻 / 易经卦象 | 卦名+动爻+世应/用神+起卦时间 | 世应、用神、动爻、生克、应期 | `yijing-divination-framework.md` |
| 8 | 梅花易数 | 起念时间/数字/方位/事件 | 体用、象数、生克、变化趋势 | `yijing-divination-framework.md` |
| 9 | 风水 / 空间 | 空间类型+布局+朝向+改善目标 | 明暗动静、收纳、床位/桌位/气口 | `fengshui-and-timing-framework.md` |
| 10 | 择时 / 窗口 | 事项+时间范围 | 适合沟通/推进/观察的节奏 | `fengshui-and-timing-framework.md` |
| 11 | 关系合盘 / 复合 | 双方档案+关系阶段+问题 | 双方状态、吸引、阻力、窗口、动作 | `relationship-and-timing.md` |
| — | 综合解读 | 上述任一组合 | 共同结论+分体系印证+分歧说明 | `SKILL.md` 总流程 |

### 1.2 从框架中提炼出的"领域数据结构"（这是真正能对接的东西）

技能虽无代码，但其文本隐含了清晰的数据模型，应 1:1 落到小程序的实体设计里：

**① 资料完整度分级模型（S/A/B/C）** —— `intake-and-routing.md`
- S 级：已有盘面/牌阵/卦盘/户型图截图，可精读。
- A 级：生日+时辰+地点+问题齐全，标准版。
- B 级：只有生日或只有星座/属相，轻量版。
- C 级：只有问题无资料，优先塔罗/梅花/综合象征解读。
- ⇒ 对应小程序里的 `tier` 字段 + 输入校验逻辑。

**② 出生档案模型 `BirthProfile`**
```
{ calendar: 'solar'|'lunar', date: 'YYYY-MM-DD', hour: 0-23,
  minute?: 0-59, birthplace: string, gender?: 'M'|'F', timezone?: string }
```

**③ 测算请求模型 `ReadingRequest`**
```
{ system: 'bazi'|'ziwei'|'tarot'|...|'composite',
  topic: 'love'|'career'|'wealth'|'study'|'family'|'health'|'relation'|'timing',
  tier: 'S'|'A'|'B'|'C',
  birth?: BirthProfile, birthPartner?: BirthProfile,
  material?: {...}, // 牌阵/卦象/盘面截图等
  question?: string }
```

**④ 解读结果模型 `ReadingResult`**（对齐 `output-templates.md` 的 10 套模板）
```
{ summary, rootCause, domains: {love?,career?,wealth?,...},
  timing: {near, mid, later}, advice: string[], closingLine,
  safetyTag: 'entertainment' }
```

**⑤ 安全护栏模型 `Guardrails`** —— `safety-and-ethics.md`
- 禁医疗/法律/投资替代、禁恐吓、禁收费改命、禁自伤/报复引导、未成年人不宿命化标签。

### 1.3 已有的"逻辑接口能力"（逻辑契约，非 HTTP API）

| 逻辑函数 | 输入 | 输出 | 来源 |
|----------|------|------|------|
| `routeSystem(req)` | 用户意图 | 选定体系（或菜单） | `SKILL.md` 体系分流 |
| `assessTier(material)` | 资料完整度 | S/A/B/C | `intake-and-routing.md` |
| `selectFramework(system, tier)` | 体系+等级 | 对应 framework 文本 | 各 framework |
| `generate(req)` | ReadingRequest | ReadingResult | 总流程 + output-templates |
| `applyGuardrails(text)` | 解读文本 | 合规文本 + safetyTag | `safety-and-ethics.md` |

### 1.4 可直接复用的资产（落到小程序时的"免费午餐"）
- 体系分流菜单、S/A/B/C 分级规则 → 直接变成**前端问卷 + 后端路由**。
- 13 套 framework 文本 → 直接变成 **AI 解读的 system prompt 知识库**（无需自己写命理知识）。
- 10 套 output-templates → 直接变成 **后端返回 JSON 的 schema + 前端卡片模板**。
- safety-and-ethics → 直接变成 **内容安全审核中间件**。

> 也就是说：你不必从零写命理知识，**`fortune-master-pro-dao-v2` 就是现成的"命理领域知识层"**。你要写的只是"排盘算法 + 把这些知识喂给 AI 的管道 + 前端交互"。

---

## 2. 需求拆解与功能模块划分

> 注：技能本身**不含"生肖运势"模块**。但生肖运势是小程序引流利器，且可由"出生年 → 生肖 + 流年干支"低成本派生，故列为**新增补充模块（MVP 可后置）**。

### 2.1 功能模块总览（按优先级）

| 模块 | 优先级 | 测算方式 | 是否需要精确算法 | 依赖 |
|------|--------|----------|------------------|------|
| 八字排盘 + AI 解读 | **P0** | 确定性排盘 + AI | 是（干支/节气/十神/大运） | 排盘库 |
| 紫微斗数 + AI 解读 | **P0** | 确定性排盘 + AI | 是（星曜落宫/四化） | 排盘库 |
| 用户档案 / 测算历史 | **P0** | — | 否 | 数据库 |
| 生肖运势（年/月） | P1 | 派生（生肖+流年） | 轻算法 | 排盘库 |
| 塔罗占卜 | P1 | AI 框架解读（牌义静态数据） | 否（牌库静态） | 塔罗牌库 |
| 西方星盘 | P1 | 排盘 + AI | 是（星历） | 排盘库 |
| 数字命理 | P1 | 纯日期运算 + AI | 否 | — |
| 奇门 / 六爻 / 梅花 | P2 | AI 框架解读（B/C 级象征式） | 否（先不做精确盘） | AI |
| 风水 / 择时 | P2 | AI 框架解读 | 否 | AI |
| 关系合盘 / 复合 | P1 | 双方档案 + AI | 复用八字/星盘 | 排盘库 |
| 会员 / 付费 / 分享海报 | P2 | 业务 | 否 | 支付 |
| 每日一签 / 运势日历 | P2 | 内容 | 否 | — |

### 2.2 模块 × 优先级 × 依赖矩阵

```
P0（MVP 必做）: 八字, 紫微, 用户体系
P1（增长期）:   生肖运势, 塔罗, 星盘, 数字命理, 关系合盘
P2（成熟期）:   奇门/六爻/梅花, 风水/择时, 会员付费, 每日一签
```

**关键洞察（来自技能本身的设计）**：技能明确允许"无盘面 → 象征性/轻量解读"。
⇒ 八字/紫微/星盘这类**需要精确排盘的体系做确定性算法**；塔罗/奇门/六爻/梅花/风水/择时这类**先用 AI 框架解读（无需写复杂算法）**，MVP 即可上线，后期再补精确算法。这把算法工作量砍掉一半以上。

---

## 3. 技术栈选型建议

### 3.1 主推荐路线：微信小程序原生 + 微信云开发（CloudBase）

| 层 | 选型 | 理由 |
|----|------|------|
| 前端 | 微信小程序原生（WXML/WXSS/TypeScript） | 最稳、审核最顺、你已有微信生态经验（猫狗大战） |
| 后端 | 微信云开发：云函数(Node 18) + 云数据库 + 云存储 | 零服务器运维、按量计费、天然小程序鉴权 |
| 排盘算法 | `lunar-javascript`（npm：lunar-javascript，作者 6tail） | 一个库覆盖八字/紫微/星座/节气/干支/生肖，久经验证 |
| AI 解读 | LLM API（混元 / 通义 / DeepSeek，兼容 OpenAI 协议） | 把 skill 的 framework 文本作 system prompt 注入 |
| 内容安全 | 微信 `security.msgSecCheck` + 自建护栏中间件 | 命理类必过内容审核 |
| 备选跨端 | uni-app（Vue3）+ Taro | 若后续要发抖音/支付宝，一套代码多端 |

### 3.2 为什么这么选
- **运维成本最低**：云开发免去买服务器、域名备案、HTTPS、负载均衡。
- **合规最顺**：微信对"算命/占卜"类目审核严，原生小程序 + 云开发 + 接入微信内容安全，过审路径最短。
- **算法最省**：`lunar-javascript` 一个库解决所有精确排盘，不必自研天文历法。
- **知识层零成本**：AI 解读直接复用 `fortune-master-pro-dao-v2` 的 framework，不必自己攒命理语料。

### 3.3 关键依赖库清单
```
前端:  @vant/weapp (UI), dayjs (时间), 
后端:  lunar-javascript (排盘), openai (LLM SDK), 
       wx-server-sdk (云开发), xml2js (如需)
静态数据: tarot-deck.json (78 张塔罗牌义), 
         ziwei-star-table.json (紫微星曜表)
```

---

## 4. 项目目录结构规划（uni-app/云开发示例）

```
mingli-mini/                      # 小程序工程根
├── cloudfunctions/               # 云函数（后端）
│   ├── pan/                      # 排盘引擎：调用 lunar-javascript
│   │   └── index.js              #   bazi/ziwei/astrology 排盘入口
│   ├── reading/                  # AI 解读服务
│   │   ├── index.js              #   编排：排盘结果 + 知识层 → LLM → JSON
│   │   ├── knowledge/            # ★ 由 skill 的 references 提升而来
│   │   │   ├── bazi.md           #   ← bazi-framework.md
│   │   │   ├── ziwei.md          #   ← ziwei-framework.md
│   │   │   ├── tarot.md          #   ← tarot-framework.md
│   │   │   ├── ... (13 个体系各一)
│   │   │   ├── routing.md        #   ← intake-and-routing.md（体系分流+分级）
│   │   │   ├── output-schema.json#   ← output-templates.md（JSON 化）
│   │   │   └── guardrails.md     #   ← safety-and-ethics.md
│   │   └── router.js             #   routeSystem()/assessTier() 逻辑实现
│   ├── zodiac/                   # 生肖运势（派生模块）
│   ├── pay/                      # 会员/付费
│   └── common/                   # 共享：鉴权、日志、安全审核
├── miniprogram/                  # 小程序前端
│   ├── pages/
│   │   ├── home/                 # 首页/体系入口
│   │   ├── intake/               # 资料采集问卷（绑定 S/A/B/C 分级）
│   │   ├── bazi/  ziwei/  tarot/ ...  # 各体系结果页（卡片化渲染 output-templates）
│   │   ├── profile/              # 用户档案/历史
│   │   └── vip/                  # 会员
│   ├── components/               # 通用卡片/牌阵/盘绘制组件
│   ├── services/                 # 前端 API 封装（调用云函数）
│   ├── store/                    # 用户状态
│   └── utils/
├── dataset/                      # 静态数据
│   ├── tarot-deck.json           # 78 张塔罗
│   └── ziwei-stars.json
├── docs/plans/                   # 本文档及后续迭代计划
└── README.md
```

> **对接要点**：`cloudfunctions/reading/knowledge/` 目录 = 技能"现有 dao 层（知识层）"在小程序里的落点。每个 `references/*.md` 对应一个知识文件；`router.js` 把"体系分流+分级"代码化；`output-schema.json` 把 output-templates 变成强类型契约。

---

## 5. 核心测算逻辑与"现有知识层"的对接方式

### 5.1 两层分离架构（最重要设计）

```
┌─────────────┐      ┌──────────────────────┐      ┌──────────────────┐
│  前端采集     │ ──▶ │  排盘层 (Pan)          │ ──▶ │  结构化盘数据      │
│ BirthProfile │      │  lunar-javascript     │      │  (确定性,JSON)    │
└─────────────┘      └──────────────────────┘      └────────┬─────────┘
                                                            │
                                                            ▼
┌─────────────┐      ┌──────────────────────┐      ┌──────────────────┐
│  前端渲染     │ ◀── │  AI 解读层 (Reading)   │ ◀── │  skill 知识层      │
│ output卡片   │      │  LLM + system prompt  │      │  (framework文本)  │
└─────────────┘      └──────────────────────┘      └──────────────────┘
```

- **排盘层**：确定性、可单测、与 AI 无关（八字四柱、紫微星盘、星盘行星）。
- **AI 解读层**：把"排盘结果 + 用户问题 + 对应 framework 文本"喂给 LLM，产出 `ReadingResult` JSON。
- **知识层**：就是 `fortune-master-pro-dao-v2` 的 framework 文本（见 5.2）。

### 5.2 把 Markdown 知识层"提升"为可消费资产

| 技能文件 | 小程序落点 | 消费方式 |
|----------|-----------|----------|
| `intake-and-routing.md` | `router.js` + 前端问卷 | 代码化：体系菜单、S/A/B/C 判定 |
| `bazi/ziwei/...-framework.md` | `knowledge/*.md` | 作为 system prompt 片段按体系注入 |
| `output-templates.md` | `output-schema.json` | 约束 LLM 输出 JSON 结构（function calling / JSON mode） |
| `safety-and-ethics.md` | `guardrails.md` + 审核中间件 | 系统提示 + 后置正则/关键词拦截 |
| `SKILL.md` 总流程 | `orchestrator` | 编排"先分流→再定级→选框架→生成→护栏" |

### 5.3 对接伪代码（云函数 `reading/index.js`）

```ts
import { panBazi } from './pan';
import { buildSystemPrompt } from './knowledge';   // 由 framework md 拼装
import { assessTier, routeSystem } from './router'; // 由 intake-and-routing 实现
import { applyGuardrails } from './guardrails';     // 由 safety-and-ethics 实现
import { chat } from 'openai';

export async function reading(req: ReadingRequest): Promise<ReadingResult> {
  const system = routeSystem(req);          // 体系分流
  const tier   = assessTier(req.material);  // S/A/B/C 分级

  // 1) 确定性排盘（仅八字/紫微/星盘需要）
  let chart = null;
  if (['bazi','ziwei','astrology'].includes(system)) {
    chart = await panBazi(req.birth);        // lunar-javascript
  }

  // 2) 拼装 system prompt：知识层 = 对应 framework 文本 + 分级规则 + 输出 schema
  const sysPrompt = buildSystemPrompt(system, tier);

  // 3) 调 LLM，要求按 output-schema.json 返回 JSON
  const raw = await chat({
    model: 'hunyuan-standard',
    messages: [
      { role: 'system', content: sysPrompt },
      { role: 'user',   content: JSON.stringify({ chart, question: req.question, topic: req.topic }) }
    ],
    response_format: { type: 'json_schema', json_schema: OUTPUT_SCHEMA }
  });

  // 4) 安全护栏（后置）
  const safe = applyGuardrails(raw);
  return { ...safe, safetyTag: 'entertainment' };
}
```

> 这一层**完整复刻了技能的"总流程"**：分流 → 定级 → 选框架 → 生成 → 护栏。区别只是从"人读 Markdown"变成"代码读 Markdown 再驱动 LLM"。

---

## 6. 前后端数据流转设计

### 6.1 核心数据模型（云数据库集合）

| 集合 | 字段 | 说明 |
|------|------|------|
| `users` | openid, createdAt | 微信用户 |
| `profiles` | userId, birth:BirthProfile, gender | 用户档案（一次录入复用） |
| `readings` | userId, system, topic, tier, chart, result, createdAt | 测算单/历史 |
| `orders` | userId, item, amount, status | 会员/付费 |
| `tarot_deck` | id, name, upright, reversed, keywords | 塔罗静态库 |

### 6.2 核心接口（云函数即 API）

| 接口 | 入参 | 出参 |
|------|------|------|
| `POST /pan/bazi` | BirthProfile | 四柱/五行/十神/大运 JSON |
| `POST /reading` | ReadingRequest | ReadingResult JSON |
| `GET  /zodiac?year=` | 年份 | 生肖+流年运势 |
| `POST /tarot/draw` | 牌阵类型 | 抽牌结果（前端随机或后端） |
| `GET  /profile` | — | 用户档案 |
| `POST /pay/create` | item | 支付单 |

### 6.3 端到端时序（以"八字测算"为例）

```
用户选「八字」→ 填出生信息(intake 页, 前端做 S/A/B/C 预判)
  → 前端调云函数 pan/bazi（排盘, 返回 chart JSON, 存 readings）
  → 前端调云函数 reading（chart+问题 → AI 解读, 返回 ReadingResult）
  → 前端按 output-templates 卡片化渲染（总断/原因/分领域/时间节奏/建议/点醒句）
  → 内容安全审核(security.msgSecCheck)通过后展示
  → 写入 readings 历史, 可分享海报
```

### 6.4 解读结果 JSON Schema（对齐 output-templates）

```json
{
  "summary":     "string (总断, 1-3 句)",
  "rootCause":   "string (底层原因)",
  "domains": {
    "love":   "string?", "career": "string?", "wealth": "string?",
    "study":  "string?", "family": "string?", "relation": "string?"
  },
  "timing": { "near": "string", "mid": "string", "later": "string" },
  "advice": ["string", "string", "string"],
  "closingLine": "string (点醒句)",
  "safetyTag": "entertainment",
  "tier": "S|A|B|C"
}
```

---

## 7. 开发阶段排期与里程碑

> 假设 1 名全栈 + 1 名前端，或你本人 + 外包/元宝协作。按双周迭代。

| 里程碑 | 周期 | 范围 | 交付物 | 验收标准 |
|--------|------|------|--------|----------|
| **M0 立项冻结** | 第 1 周 | 需求确认、合规自查、技术栈定稿 | 本文档 + PRD | 功能清单/优先级签字 |
| **M1 排盘内核** | 第 2-4 周 | lunar-javascript 接入；八字/紫微/星盘排盘 + 单测 | `pan` 云函数 | 排盘结果与权威排盘工具对照一致 |
| **M2 前端骨架** | 第 3-4 周 | 首页/体系入口/intake 问卷/档案/历史 | 可跑小程序壳 | 能录入出生信息并调通 pan |
| **M3 AI 解读打通** | 第 5-6 周 | knowledge 层落地 + reading 云函数 + 护栏 | 八字/紫微端到端 | 返回合规 ReadingResult JSON |
| **M4 内容模块扩展** | 第 7-9 周 | 生肖/塔罗/星盘/数字/关系合盘 | 多体系可用 | 各体系产出结构化解读 |
| **M5 安全与合规** | 第 9-10 周 | 内容安全、未成年人、付费边界、隐私 | 合规清单通过 | 过微信内容安全 + 类目资质 |
| **M6 商业化** | 第 11-12 周 | 会员/付费/分享海报/每日一签 | 上线候选版 | 支付联调通过、分享可用 |
| **M7 灰度上线** | 第 13 周+ | 小流量 → 全量 → 迭代 | 正式版 | 留存/转化达标，持续迭代 |

**关键路径**：M1 排盘内核 是最大技术风险点，应最先启动并单测验证。

---

## 8. 合规与上线清单（命理类小程序必过）

微信对"算命/占卜/风水"类目审核极严，务必提前准备：

1. **类目资质**：需选对应类目（如"咨询-命理"），部分需《增值电信业务经营许可证》或文化类资质；提前查最新类目要求。
2. **内容安全**：所有 AI 生成文本必须过 `security.msgSecCheck`；自建关键词/正则护栏（复用 `safety-and-ethics.md`）。
3. **明确娱乐声明**：每个解读结果强制带 `safetyTag: 'entertainment'` 与"结果仅供娱乐参考，不构成专业建议"文案。
4. **禁付费改运**：不得提供"消灾/化解/改命"收费服务；会员只能卖"解读次数/高级模板"，不能卖"改运"。
5. **禁医疗/投资/法律替代**：护栏已覆盖，前端也加醒目提示。
6. **未成年人保护**：不输出宿命化标签；按 `safety-and-ethics.md` 第 5 条处理。
7. **隐私**：出生信息属敏感个人信息，明示授权、加密存储、可删除。

---

## 9. 立即可以开始的第一步（Next Action）

按 Superpowers 工作流，下一步进入 **Brainstorm → Plan** 的落地动作。建议你（或接手工程师）从下面任选一条启动：

1. **最小验证（1 天）**：在云函数里 `npm i lunar-javascript`，写 10 行代码排出一个八字四柱，与"查八字"类工具对照，确认排盘内核可行。**这是最大技术风险的拆弹**。
2. **知识层落地（半天）**：把 `fortune-master-pro` 的 13 个 `references/*.md` 复制到 `cloudfunctions/reading/knowledge/`，并把 `output-templates.md` 转成 `output-schema.json`。**这一步零编码、纯搬运，立刻拥有"命理知识层"**。
3. **原型串联（2-3 天）**：前端 intake 页 → `pan/bazi` → `reading`（注入 knowledge）→ 卡片渲染，打通第一条端到端链路。

> 一旦 M1+M3 验证通过，后续模块（塔罗/星盘/数字/生肖/关系）只是"加 knowledge 文件 + 加前端页"的重复劳动，边际成本极低。

---

### 附：本方案与技能的对应关系一览

| 规划章节 | 来源（技能文件） |
|----------|------------------|
| 模块划分 | `SKILL.md` 体系分流 + `README.md` 适用场景 |
| S/A/B/C 分级 | `intake-and-routing.md` |
| 各体系字段 | 13 个 `*-framework.md` |
| 输出结构 | `output-templates.md`（10 套模板） |
| 安全护栏 | `safety-and-ethics.md` |
| 综合交叉验证 | `SKILL.md` 多体系增强规则 |
| 关系/复合逻辑 | `relationship-and-timing.md` |
| 道家玄学分流 | `dao-mysticism-framework.md` + `qimen`/`yijing`/`fengshui` |
