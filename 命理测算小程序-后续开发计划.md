# 命理测算小程序 — 后续开发计划（Roadmap）

> 基线：MVP 已交付（Web 版，八字/生肖/塔罗 + 知识层 + AI 解读层 + 前端 + 测试 + GitHub 远程）。
> 本计划在其上扩展为**完整可上线的小程序**，保持既有的「排盘层 / 知识层 / AI 解读层」三层分离架构。

---

## 一、总目标与范围

### 1.1 总目标
在 MVP 基础上，补齐核心测算体系、升级 AI 解读、产出**微信小程序壳**、满足**上线合规**，形成可被用户实际使用与商业化的命理测算产品。

### 1.2 范围界定
**包含（In Scope）**
- 算法排盘扩展：紫微斗数、数字命理、每日运势
- 玄学体系：六爻/梅花易数、奇门遁甲、风水择时、关系合盘
- AI 解读层升级：LLM 接入、知识注入、多轮对话、安全护栏
- 微信小程序前端（复用同一套 API）
- 内容安全审核、娱乐声明、未成年人保护、合规类目适配
- 运营模块：收藏/历史、分享卡片、年运/月运
- 测试、CI、文档、GitHub 版本纪律

**不含（Out of Scope）**
- 付费改运、消灾收费等违规业务（明确禁止）
- 医疗/法律/财务专业诊断（安全护栏禁止）
- iOS/Android 原生 App（本期仅微信小程序 + Web）
- 自建命理大模型训练（本期仅做提示词/知识注入）

### 1.3 技术约束
- **排盘准确性**：干支/历法以 `lunar-javascript` 为准；自研公式须与权威工具交叉校验。
- **零/低依赖**：后端保持 Node 内置能力为主，仅引入必要排盘/LLM 库。
- **密钥管理**：LLM Key、微信 AppSecret 等**不入库**、不进前端，走环境变量/云函数配置。
- **小程序体积**：主包 ≤ 2MB、整包（含分包）≤ 20MB；知识文档不放前端包，改由 API 下发。
- **合规护栏**：所有解读强制携带娱乐声明，禁止恐吓式/改运收费话术（沿用 `safety-and-ethics.md`）。

---

## 二、架构与依赖关系

```
前端层:  Web(H5)  ─┐
       小程序(WXML) ─┼─→ 统一 API 网关(/api/*) ─→ AI 解读层 ─→ 知识层(13框架)
                    │                      │
排盘层: lunar-javascript + 自研算法(紫微/数秘/易卦) 
                    │
合规层: 内容安全审核 + 娱乐声明 + 未成年人保护
```

**关键依赖**
- 排盘层 → `lunar-javascript`（已装）；紫微/星盘若 lunar 覆盖不足，引入专项库或自研（需评估）。
- 小程序壳 → 需**用户侧**提供微信 AppID + 开发者工具（本机无法运行真机预览）。
- AI 解读层 → 需**用户侧**提供 LLM API Key（混元/DeepSeek/通义等兼容 OpenAI 协议）。
- 版本纪律 → 每次里程碑需**用户提供一个 `repo` 级 PAT**（当前 GitHub 集成无法建仓/直推，见 `github-push-with-pat` skill）。

---

## 三、里程碑总览

| 阶段 | 目标 | 优先级 | 预计周期 | 主要交付 | 验收要点 |
|---|---|---|---|---|---|
| M1 | 算法排盘扩展 | P0 | W1–W2 | 紫微/数字命理/每日运势模块 | 三体系可算、测试通过 |
| M2 | 玄学体系补全 | P1 | W3 | 六爻/梅花/奇门/风水择时/关系合盘 | 框架+AI 解读可用 |
| M3 | AI 解读升级 | P0 | W4 | LLM 适配器+知识注入+多轮 | 解读质量提升、可评测 |
| M4 | 微信小程序壳 | P0 | W5–W6 | 小程序工程+真机预览 | 三大模块在真机跑通 |
| M5 | 运营与内容 | P1 | W7 | 收藏/历史/分享/年运推送 | 留存能力就绪 |
| M6 | 合规与安全 | P0 | W8 | 内容审核+声明+未成年保护 | 过审前置条件满足 |
| M7 | 商业化与上线 | P2 | W9 | 会员/广告/统计/灰度 | 可灰度发布 |

> 周期以「周」为相对单位（自 2026-08-13 起），可按实际人力压缩；每阶段结束做一次 commit + 远程 push。

---

## 四、各阶段详细拆解

### M1 — 算法排盘扩展（P0 · W1–W2）
**目标**：补齐三个确定性/半确定性测算模块。
- [ ] **紫微斗数**：评估 `lunar-javascript` 的 `ZiWeiDouShu` 覆盖；不足则自研 14 主星安星 + 12 宫。产出 `src/pan/ziwei.js`。
- [ ] **数字命理**：生命灵数/天赋数（生日数理），产出 `src/pan/numerology.js`。
- [ ] **每日运势**：基于生肖+当日干支的趣味日运，产出 `src/pan/daily.js`。
- [ ] 对应 API：`/api/ziwei`、`/api/numerology`、`/api/daily`。
- [ ] 前端新增 Tab 与卡片模板。
- [ ] 单元测试 + 与权威工具交叉校验。
**交付**：3 个新模块 + 接口 + 前端 + 测试；GitHub 提交+推送。
**验收**：① 紫微十二宫/主星输出非空且结构稳定；② 数字命理公式可解释；③ 日运可复现；④ `node test/run.js` 全绿。

### M2 — 玄学体系补全（P1 · W3）
**目标**：用「框架 + AI 解读」补齐象征性体系（skill 允许 B/C 级无盘象征式）。
- [ ] **六爻/梅花易数**：起卦（掷钱/时间/数字）+ 卦象解析，产出 `src/pan/yijing.js`。
- [ ] **奇门遁甲**：排盘（局数/门星神），产出 `src/pan/qimen.js`（或纯 AI 框架）。
- [ ] **风水与择时**：基础罗盘/吉凶方位/择日，产出 `src/pan/fengshui.js`。
- [ ] **关系合盘**：两人八字/生肖合参，产出 `src/pan/relationship.js`。
- [ ] 知识层补充对应框架（已有 references，做索引映射）。
- [ ] AI 解读层增加上述体系的 prompt 模板。
**交付**：4 个模块 + 知识索引 + 解读模板 + 前端入口。
**验收**：① 各体系可输入并产出结构化结果；② AI 解读引用对应框架且不越护栏；③ 测试覆盖。

### M3 — AI 解读升级（P0 · W4）
**目标**：从规则模板升级为高质量 LLM 解读，保留离线回退。
- [ ] **LLM 适配器**：统一接口（混元/DeepSeek/通义），`LLM_API_*` 环境变量驱动；失败自动回退规则。
- [ ] **知识注入**：按体系拼接 `buildKnowledgePack(system)`，控制 token 长度。
- [ ] **多轮对话**：会话上下文记忆（用户追问日主/大运细节）。
- [ ] **质量评测**：10 条样例断言（结构完整、无违规、引用正确）。
- [ ] **护栏强化**：输出前过滤恐吓/改运/医疗话术。
**交付**：`src/ai/llm.js` + 评测脚本 + 文档。
**验收**：① 配 Key 后解读明显优于规则版；② 无 Key 时规则版正常；③ 评测样例全部通过；④ 违规话术 0 命中。

### M4 — 微信小程序壳（P0 · W5–W6）
**目标**：把 Web 前端改造为微信小程序，复用同一 API。
- [ ] **小程序工程**：`miniprogram/` 下 `app.js/app.json` + 页面（八字/生肖/塔罗/新模块）。
- [ ] **网络层**：`wx.request` 封装，对接后端 API（自托管或云函数）。
- [ ] **样式**：WXSS 还原移动端视觉；tabBar/分享。
- [ ] **云函数（可选）**：将 Node 服务部署为微信云函数，免运维。
- [ ] **真机预览**：需用户提供 AppID，在微信开发者工具预览（本机无法跑真机）。
**交付**：可预览的小程序工程 + 对接文档。
**验收**：① 三大核心模块在模拟器/真机跑通；② 网络请求成功；③ 分包体积达标；④ 分享卡片正常。

### M5 — 运营与内容（P1 · W7）
- [ ] 用户收藏/历史（本地 + 可选云端）。
- [ ] 分享卡片生成（canvas）。
- [ ] 生肖年运/月运/每日推送（订阅消息）。
- [ ] 首页改版与模块导航。
**验收**：① 收藏/历史可增删查；② 分享卡片可生成；③ 推送可触发。

### M6 — 合规与安全（P0 · W8）
- [ ] **内容安全审核**：接入微信 `msgSecCheck` 或自建敏感词，覆盖用户输入与 AI 输出。
- [ ] **娱乐声明**：全局固定文案。
- [ ] **未成年人保护**：年龄提示/功能限制。
- [ ] **类目资质**：梳理微信「运势」类目所需资质（如《增值电信业务经营许可证》等），输出清单。
**验收**：① 用户输入违规被拦截；② AI 输出含声明；③ 未成年路径受限；④ 资质清单齐备。

### M7 — 商业化与上线（P2 · W9）
- [ ] 会员/付费解读（仅合规项目）。
- [ ] 广告位（合规）。
- [ ] 数据统计（UV/留存/转化）。
- [ ] 灰度发布与回滚预案。
**验收**：① 付费流程跑通；② 统计埋点生效；③ 灰度可回滚。

---

## 五、验收标准总表（跨阶段）
- **功能**：每个模块输入→结构化输出→前端展示链路完整。
- **质量**：`node test/run.js` 全绿；新增模块均有单测；M3 后 LLM 评测样例全过。
- **安全**：所有 AI 输出携带娱乐声明；违规话术 0 命中；密钥不入库。
- **合规**：M6 完成方可提交微信审核；资质齐备。
- **版本**：每阶段结束本地 commit + 远程 push（需用户 PAT），`main` 始终可运行。

---

## 六、风险与合规提示
- **微信「运势」类目审核严格**：纯娱乐测算常因「诱导分享/虚假宣传/无资质」被拒；M6 必须前置处理，并准备「工具/查询」类目替代方案。
- **LLM 依赖外部 Key**：用户需提供；无 Key 时规则版兜底，不影响基本可用。
- **小程序真机预览依赖用户侧 AppID**：M4 无法在本机独立完成真机验证。
- **GitHub 推送依赖用户 PAT**：每次里程碑需用户重新提供（当前集成不能直推）。

---

## 七、执行与跟踪纪律
1. 每阶段拆为可勾选任务清单（上文 `- [ ]`）。
2. 阶段内小步提交；阶段结束 `git commit` + 远程 `push`。
3. 每个可运行成果对应一个 GitHub commit 与远程记录。
4. 进度以本文件 + GitHub 提交历史为单一事实来源。
