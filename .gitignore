# dependencies
node_modules/

# local env / secrets
.env
.env.local
*.local

# workbuddy internal (never commit)
.workbuddy/

# os / editor
.DS_Store
Thumbs.db
*.log

# test artifacts
coverage/

# local user data (history/favorites store)
data/

# build output (generated by `node tools/build-demo.js`)
dist/

# 本地输出根目录（npm 缓存 / 日志 / 临时文件，已由 wb.config.json 配置重定向）
.wb-output/

