# 命理测算小程序 — 交付说明（持续交付中）

## 成果
一个**可运行的命理测算 Web 应用**（三层架构：排盘层 / 知识层 / AI 解读层），已实现并自动化测试验证（27 项测试全绿）。

### 已交付功能
- **八字排盘** `/api/bazi`：四柱、干支、五行、十神、藏干、纳音、大运、胎元/命宫
- **紫微斗数** `/api/ziwei`：命宫/身宫、五行局、十四主星、十二宫（简版）
- **生肖运势** `/api/zodiac`：本命/冲/合关系判定
- **每日运势** `/api/daily`：当日干支、冲合、评级、幸运色
- **数字命理** `/api/numerology`：生命灵数（含 11/22/33 主数）、天赋/生日/缺失数
- **塔罗占卜** `/api/tarot`：大阿卡纳 22 张，单张/三张（过去/现在/未来）
- **六爻起卦** `/api/yijing`：时间/数字起卦，六十四卦释义
- **奇门择吉** `/api/qimen`：阴阳遁、局数、八门吉凶（演示版，节令近似）
- **风水八宅** `/api/fengshui`：本命卦、东四/西四命、吉凶方位、择日
- **关系合盘** `/api/relationship`：生肖六合/三合/冲/害/刑 + 年命五行生克评分
- **年运** `/api/annual`：流年生肖关系 + 当年 12 个月逐月概要、最佳/最需谨慎月
- **月运** `/api/monthly`：指定年月流月关系与运势提示
- **星盘占星** `/api/xingzhan`：西方占星，基于 `astronomy-engine` 真实星历计算十大行星地心黄道经度、落座与度数、顺/逆行、五大主要相位（合/六分/四分/三分/对冲）
- **用户存储** `/api/user/history` `/api/user/favorites`：按 `X-Client-Id` 分用户保存历史与收藏（本地 JSON，上限 50）
- **AI 解读层** `/api/reading`：规则模板（零依赖离线）+ LLM 可插拔（设 `LLM_API_KEY/URL` 自动切换）；支持 `deep:true` 会员深度解读
- **会员 / 商业化**（M7）：`/api/membership`（状态查询）+ `/api/membership/upgrade`（模拟开通）；深度解读仅会员可调用（403 鉴权）。Web「会员」Tab + 小程序「会员」页已接入。**当前为本地演示，未接真实支付**
- **前端（Web）**：原生 HTML/CSS/JS 单页（移动端风格），含 13 个测算 Tab + 历史/收藏记录中心 + 八字分享卡
- **前端（小程序）**：`miniprogram/` 微信小程序壳，数据驱动接入 13 个体系；测算结果自动历史 + 一键收藏 + 会员「深度解读」入口 + 会员页 + 服务设置页（可视化配置后端地址并连通性自检）+ 分享 + 免责页。**功能已补齐完整**（详见 README「微信小程序壳」一节）。
- **知识层**：移植自 `fortune-master-pro-dao-v2` 的 13 套体系框架文档

## 技术选型
Node.js 内置 `http` 服务（**零额外依赖**，仅 `lunar-javascript`（东方针算）与 `astronomy-engine`（西占星历）用于排盘）；前端原生三件套。可直接套微信小程序 WebView 或后续改造为小程序。

## 运行
```bash
npm install && npm test && npm start   # http://localhost:3000
```

## 里程碑进度
| 阶段 | 内容 | 状态 |
|---|---|---|
| MVP | 八字/生肖/塔罗 + 知识层 + 解读层 + 前端 + 测试 | ✅ 已交付 |
| M1+M3 | 紫微/数秘/日运 + LLM 解读适配器 | ✅ 已交付 |
| M2 | 六爻/奇门/风水/合盘 | ✅ 已交付 |
| M5 | 年运/月运 + 用户存储 + 前端记录中心 + 分享卡 | ✅ 已交付 |
| M4 | 微信小程序壳：13 体系菜单 + 测算页 + 记录页 + **收藏/深度解读/会员页/服务设置页/分享/免责页**（功能已补齐完整） | ✅ 已交付（待用户填真实 AppID + 配 HTTPS 域名白名单） |
| M6 | 合规与安全：娱乐声明/未成年人保护/内容安全（已落地） + 平台类目资质（需用户办理） | 🟡 合规层已实现，类目资质待用户 |
| 星盘 | 第 13 个体系：西方占星（astronomy-engine 真实星历，十大行星落座+相位+顺逆行）+ 前后端（Web/小程序）接入 + 测试 | ✅ 已交付（已推送远程） |
| M7 | 商业化与上线：Freemium 会员模型（深度解读专享）+ Web/小程序会员页 + 部署与上线指南 + 合规红线说明 | ✅ 商业化框架已落地（演示态，待接真实支付/资质） |

## 版本控制
- 本地 git：`main` 分支，已提交 MVP / M1+M3 / M2 / M5 / M4 / M6 / 星盘 / M7 等提交。
- 远程 GitHub：`https://github.com/qwf554655054-ship-it/fortune-master-miniprogram`（**已推送同步**，HEAD `618134f`；本地 `.git/config` 不含凭据，后续建议改 SSH 免 PAT 推送）。
- **在线演示（纯静态、无后端）：** https://52450abfdcc2403aadd3756b28f77c57.app.workbuddy.link — 用 esbuild 把排盘+解读引擎打包进浏览器，前端 `shim.js` 拦截 `/api/*` 本地计算（构建：`node tools/build-demo.js`）。演示态：规则解读 / 历史收藏不持久化 / 会员演示态；完整功能用带后端的 `server.js`。

## 合规与上线风险（重要）
- **微信小程序「算命/占卜/风水」类目审核极严**：该类内容在微信开放平台通常归入受限或需特殊资质的类目（如"宗教/迷信""心理测试"，或要求《增值电信业务经营许可证》/营业执照+说明）。大量算命类小程序会被拒审或下架，务必提前评估。
- **本项目已落地的合规层**：Web 端（页头声明链接 + 免责声明弹层 + 未成年人首访提示 + 每条解读末尾统一"仅供娱乐"声明）、小程序端（首页声明含未成年提示 + 关于/免责页 + 测算结果底部声明 + 记录页入口）。技术上已满足"有娱乐声明、有未成年人保护、无付费改运/医疗承诺"的基本要求。
- **上线前仍需用户办理**：① 小程序后台「服务类目」选合规类目并提交资质；② 不以"算命/改运"为主定位，建议以"心理测试/趣味娱乐"口径降低审核风险；③ 持续内容审核，避免出现医疗、投资、婚恋保证等违禁表述。
- **建议路径**：先用 Web/H5 版本（server.js + public/）做内测与分享传播，再视微信审核情况决定小程序上线路径。
- **隐私政策 / 用户协议（小程序提审必填项）**：微信小程序上线必须在公众平台「用户隐私保护指引」填写《隐私政策》可访问 URL。已新增 `public/privacy.html` 与 `public/agreement.html` 两份可直接托管的模板（反映真实数据实践：不收集真实身份/敏感权限、按匿名 clientId 隔离、历史收藏本地存储、会员本地演示未接真实支付、未成年保护、娱乐声明、用户删除权）。两份页面已纳入 `node tools/build-demo.js` 构建产物 `dist/`，可随在线演示站一并托管，访问 `演示域名/privacy.html`、`/agreement.html` 即得上架所需 URL。

## 待办 / 风险
1. ✅ **已推送远程**：2026-08-14 用户提供新 GitHub PAT，已将本地全部提交（MVP→M1+M3→M2→M5→M4→M6→星盘→M7，共 9 个提交，HEAD `bb03f93`）一次性推送至 `https://github.com/qwf554655054-ship-it/fortune-master-miniprogram`；推送后已清除本地配置中的 token，远程 URL 不再含凭据。
2. 🔧 **SSH 免 PAT 推送已就绪（待用户最后一步）**：已生成 `~/.ssh/id_ed25519` 密钥对，并将远程改为 SSH 形式 `git@github.com:qwf554655054-ship-it/fortune-master-miniprogram.git`。因现有 GitHub 集成令牌无 `write:public_key` 权限（API 返回 404），公钥需由用户在 GitHub 网页端「Settings → SSH and GPG keys → New SSH key」手动添加（公钥见下方消息）。添加后无需任何 token 即可 `git push`。本提交 `e9f97a1` 已就位，待公钥生效后即可推送。
2. ✅ **小程序功能已补齐完整**（2026-08-14）：收藏、会员深度解读、服务设置页（可视化配置后端地址）、分享、免责页、记录页会员状态；并修复了六爻起卦表单缺年月日时的问题。新增 `test/mp-run.js` 小程序端到端测试（mock wx/getApp + 路由真实后端），**19 项全绿**；后端 `test/run.js` **27 项全绿**。
3. ⏳ M4 小程序壳需要用户填真实微信 AppID + 发布前配 HTTPS 域名白名单（工程内 `appid` 当前为 `touristappid` 占位，可在「服务设置」里配置后端地址）。
4. 微信「算命/占卜/风水」类目资质（见上方合规风险）需用户自行办理。
5. M7 会员当前为本地演示（未接真实支付）；正式商业化需接微信支付 / 苹果内购，并补齐 ICP 备案、增值电信许可证等。详见 `docs/M7-商业化与上线.md` 与 `docs/DEPLOY.md`。
