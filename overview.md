# 命理测算小程序 — 交付说明（持续交付中）

## 成果
一个**可运行的命理测算 Web 应用**（三层架构：排盘层 / 知识层 / AI 解读层），已实现并自动化测试验证（21 项测试全绿）。

### 已交付功能
- **八字排盘** `/api/bazi`：四柱、干支、五行、十神、藏干、纳音、大运、胎元/命宫
- **紫微斗数** `/api/ziwei`：命宫/身宫、五行局、十四主星、十二宫（简版）
- **生肖运势** `/api/zodiac`：本命/冲/合关系判定
- **每日运势** `/api/daily`：当日干支、冲合、评级、幸运色
- **数字命理** `/api/numerology`：生命灵数（含 11/22/33 主数）、天赋/生日/缺失数
- **塔罗占卜** `/api/tarot`：大阿卡纳 22 张，单张/三张（过去/现在/未来）
- **六爻起卦** `/api/yijing`：时间/数字起卦，六十四卦释义
- **奇门择吉** `/api/qimen`：阴阳遁、局数、八门吉凶（演示版，节令近似）
- **风水八宅** `/api/fengshui`：本命卦、东四/西四命、吉凶方位、择日
- **关系合盘** `/api/relationship`：生肖六合/三合/冲/害/刑 + 年命五行生克评分
- **年运** `/api/annual`：流年生肖关系 + 当年 12 个月逐月概要、最佳/最需谨慎月
- **月运** `/api/monthly`：指定年月流月关系与运势提示
- **用户存储** `/api/user/history` `/api/user/favorites`：按 `X-Client-Id` 分用户保存历史与收藏（本地 JSON，上限 50）
- **AI 解读层** `/api/reading`：规则模板（零依赖离线）+ LLM 可插拔（设 `LLM_API_KEY/URL` 自动切换）
- **前端**：原生 HTML/CSS/JS 单页（移动端风格），含 12 个测算 Tab + 历史/收藏记录中心 + 八字分享卡
- **知识层**：移植自 `fortune-master-pro-dao-v2` 的 13 套体系框架文档

## 技术选型
Node.js 内置 `http` 服务（**零额外依赖**，仅 `lunar-javascript` 用于排盘）；前端原生三件套。可直接套微信小程序 WebView 或后续改造为小程序。

## 运行
```bash
npm install && npm test && npm start   # http://localhost:3000
```

## 里程碑进度
| 阶段 | 内容 | 状态 |
|---|---|---|
| MVP | 八字/生肖/塔罗 + 知识层 + 解读层 + 前端 + 测试 | ✅ 已交付 |
| M1+M3 | 紫微/数秘/日运 + LLM 解读适配器 | ✅ 已交付 |
| M2 | 六爻/奇门/风水/合盘 | ✅ 已交付 |
| M5 | 年运/月运 + 用户存储 + 前端记录中心 + 分享卡 | ✅ 已交付（本次） |
| M4 | 微信小程序壳 | ⏳ 待启动（需用户微信 AppID） |
| M6 | 合规与安全（内容审核/娱乐声明/未成年保护/类目资质） | ⏳ 规划中 |
| M7 | 商业化与上线 | ⏳ 规划中 |

## 版本控制
- 本地 git：`main` 分支，已提交 MVP / M1+M3 / M2 / **M5** 四次提交。
- 远程 GitHub：`https://github.com/qwf554655054-ship-it/fortune-master-miniprogram`
  - ⚠️ **旧 PAT 已被用户撤销并轮换**，M1+M3 / M2 / M5 的提交**尚未推送至远程**。需用户提供新的 GitHub PAT 后，我会一次性推送全部本地提交。

## 待办 / 风险
1. 推送远程需新的 GitHub PAT（旧的已撤销）。
2. M4 小程序壳需要用户的微信小程序 AppID 与开发者资质。
3. 生产上线前需补充娱乐声明、未成年人保护、内容审核与平台类目资质（M6）。
